$(document).ready(function() {
  // With options:
  $('#to-watch').sortable({
  // SortableJS options go here
  // See: (https://github.com/SortableJS/Sortable#options)

    handle: '.handle',
    invertSwap: true,
    // . . .
  })

  // With options:
  $('#to-eat').sortable({
    // SortableJS options go here
    // See: (https://github.com/SortableJS/Sortable#options)

      handle: '.handle',
      invertSwap: true,
      // . . .
    })

    // With options:
  $('#to-read').sortable({
    // SortableJS options go here
    // See: (https://github.com/SortableJS/Sortable#options)

      handle: '.handle',
      invertSwap: true,
      // . . .
    })

    // With options:
  $('#to-buy').sortable({
    // SortableJS options go here
    // See: (https://github.com/SortableJS/Sortable#options)

      handle: '.handle',
      invertSwap: true,
      // . . .
    })
    
});
